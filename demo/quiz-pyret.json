{
    "title": "Emmanuel's Sample Quiz",
    "questions": [
        {
            "type": "Pyret",
            "prompt": {
                "prompt": "Change the definition of `r` so that the rectangle will be 400px wide",
                "program": "use context starter2024\n\nr = rectangle(100, 200, \"solid\", \"orange\")",
                "checkblock": "check:\nimage-width(r) is 400\nend"
            },
            "answer": "`r = rectangle(4 * 100, 200, \"solid\", \"orange\")`"
        },
        {
            "type": "MultipleChoice",
            "prompt": {
                "prompt": "What does it mean if a variable `x` is immutable?\n",
                "distractors": [
                    "`x` is stored in the immutable region of memory.",
                    "After being defined, `x` can be changed at most once.",
                    "You cannot create a reference to `x`.![alt text](./react.svg 'react logo')"
                ]
            },
            "answer": {
                "answer": ["`x` cannot be changed after being assigned to a value.", "MEEEEEEE"]
            },
            "context": "Immutable means \"not mutable\", or not changeable.\n"
        }
    ]
}